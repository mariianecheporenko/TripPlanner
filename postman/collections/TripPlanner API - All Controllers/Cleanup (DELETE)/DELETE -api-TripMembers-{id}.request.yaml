$kind: http-request
name: DELETE /api/TripMembers/{id}
url: "{{baseUrl}}/api/TripMembers/{{tripMemberId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.response.to.have.status(204);
    language: text/javascript
order: 3000
