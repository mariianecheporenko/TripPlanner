$kind: http-request
name: DELETE /api/Destinations/{id}
url: "{{baseUrl}}/api/Destinations/{{destinationId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.response.to.have.status(204);
    language: text/javascript
order: 4000
