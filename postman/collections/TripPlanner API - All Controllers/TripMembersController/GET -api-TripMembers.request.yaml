$kind: http-request
name: GET /api/TripMembers
url: "{{baseUrl}}/api/TripMembers"
method: GET
scripts:
  - type: afterResponse
    code: pm.response.to.have.status(200);
    language: text/javascript
order: 2000
