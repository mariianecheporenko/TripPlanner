$kind: http-request
name: GET /api/TripHistories
url: "{{baseUrl}}/api/TripHistories"
method: GET
scripts:
  - type: afterResponse
    code: pm.response.to.have.status(200);
    language: text/javascript
order: 2000
