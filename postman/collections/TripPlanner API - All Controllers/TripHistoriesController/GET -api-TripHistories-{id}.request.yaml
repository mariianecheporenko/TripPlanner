$kind: http-request
name: GET /api/TripHistories/{id}
url: "{{baseUrl}}/api/TripHistories/{{tripHistoryId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.response.to.have.status(200);
    language: text/javascript
order: 3000
